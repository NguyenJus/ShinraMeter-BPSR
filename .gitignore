/target
Cargo.lock.orig
*.pcap
*.pcapng
docs/specs/
docs/plans/
/dist

# Screenshots of the reference meter used to drive UI work (issue #9). They are
# captures of another project's UI, so they stay local rather than being
# redistributed here.
docs/reference/

# The name cache `main.rs` falls back to when there is no %APPDATA% — i.e. any
# time the overlay is run on a dev box rather than on Windows.
/ShinraMeter-BPSR-names.json

# Packet-inspection diagnostic dumps (issue #25, SHINRA_INSPECT=1). These
# contain player names and other identifying traffic captured from a live
# session — never commit one or attach it to an issue/PR; mint a minimal
# synthetic fixture instead.
ShinraMeter-BPSR-inspect-dump-*.jsonl
inspect-dumps/
*.inspect-dump.jsonl
